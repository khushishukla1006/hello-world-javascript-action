name: Test Hello World Composite Action

on: [push]

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: A job to say hello

    steps:
      - uses: actions/checkout@v6

      - id: foo
        uses: khushishukla1006/hello-world-composite-action@v1
        with:
          who-to-greet: 'Khushi'

      - run: echo "random-number $RANDOM_NUMBER"
        shell: bash
        env:
          RANDOM_NUMBER: ${{ steps.foo.outputs.random-number }}
